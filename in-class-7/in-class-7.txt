1.  _A CSS framework_ is a collection of CSS stylesheets that are prepped and ready to use.
2.  _Bootstrap components_ are pre-styled UI elements like buttons, forms, navbars, and more, built with
HTML, CSS, and JavaScript.
3. _Responsive design_ creates dynamic changes to the appearance of a website, depending on the
screen size and orientation of the device being used to view it.
4. _The meta tag with viewport attributes_ is needed to get media queries to work
